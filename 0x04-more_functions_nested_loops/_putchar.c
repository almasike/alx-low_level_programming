#include "main.h"
#include <unistd.h>
/**
 * _putchar - writes the character c to stdout
 * @c: The character to prirint
 *
 * Return:On succes 1.
 * On error,-1 is returned,and ermo is set appropriately
 */
int _putcher(char)
{
	returen(write(1,&c,1));
}
